# Current major versions as of 2026-09-11, verified installable and
# working together against the exact API surface these exercises use
# (StateGraph/add_conditional_edges, ToolNode, tools_condition, add_messages,
# @tool, bind_tools, with_structured_output, init_chat_model,
# OpenAIEmbeddings, langchain_chroma.Chroma). No new package beyond what
# Days 15-16 (lab/day06, lab/day07) already installed - today's course
# content is explicit that no new package or network domain is needed.
langchain>=1.3
langgraph>=1.2
langchain-openai>=1.3
langchain-chroma>=0.3
chromadb>=1.4
pydantic>=2.10
python-dotenv>=1.2
