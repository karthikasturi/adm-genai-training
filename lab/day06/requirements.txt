# Current major versions as of 2026-09-09, verified installable and
# working together against the exact API surface these exercises use
# (PromptTemplate, StateGraph/add_conditional_edges, InMemorySaver,
# add_messages, @tool, bind_tools, ToolNode, init_chat_model).
langchain>=1.3
langgraph>=1.2
langchain-anthropic>=1.4
langchain-openai>=1.3
python-dotenv>=1.2
